#include <stdio.h>
int main() 
{
    int value = 0;
    int scanf_status = 0;

    printf("Enter an integer value: ");
    scanf_status = scanf("%d", &value);
    
    printf("Status is: %d\n", scanf_status);
    printf("Number is %d\n", value);

    return 0;
}
